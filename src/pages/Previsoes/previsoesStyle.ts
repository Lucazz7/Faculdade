import styled from 'styled-components';

export const ContainerPrevisoes = styled.div`
  background: ${({ theme }) => theme.bg3};
  height: 100vh;
  width: 100%;
  display: flex;
  position: relative;
  padding: 0;
  margin: 0;
`;